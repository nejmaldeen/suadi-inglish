# تعليمات العمل على مشروع سوالف

ينطبق هذا الملف على المستودع كاملًا. قبل أي تعديل، يجب على أي محادثة جديدة قراءة الملفات التالية بالترتيب:

1. `README.md`
2. `docs/PROJECT_STATUS.md`
3. `docs/WORKSTREAMS.md`
4. `docs/ACHIEVEMENT_TRACKER.md`
5. `docs/DECISIONS.md`

ثم تفحص `git status --short` و`git log -1 --oneline` وموضع الـworktree الحالي. لا تبدأ العمل إذا لم تتطابق الغرفة والفرع والـworktree مع المهمة.

## هدف المشروع

هذا المشروع نسخة عرض سريعة لتجربة «سوالف — Saudi English Voice Showcase». الهدف الحالي هو إظهار قيمة موقف واحد بوضوح: طلب القهوة بالإنجليزية، وليس بناء المنتج الكامل.

حافظ على التسلسل المعتمد:

`listening → thinking → speaking → review`

وحافظ على الواجهة الحالية واتجاهها البصري ما لم يطلب المستخدم تغييرًا محددًا.

## نقطة الأساس

- نقطة الأساس المعتمدة قبل إضافة وثائق المشروع: `391279b3d5d58c69f99e6869bb1642f13d16aebb`.
- المستودع البعيد: `https://github.com/nejmaldeen/suadi-inglish`.
- الفرع الأساسي: `main`.
- افحص `git status` و`git log -1` قبل بدء العمل، ولا تفترض أن شجرة العمل نظيفة.
- تغييرات موجودة مسبقًا تخص المستخدم؛ لا تحذفها أو تستبدلها.

## نموذج غرف العمل

- `WS-00` وحدها تملك وثائق القيادة المركزية، وتراجع التسليمات، وتدمج إلى `main`.
- تعمل كل غرفة أخرى في branch وGit worktree مستقلين؛ لا تعمل محادثتان داخل worktree واحد.
- ملكية الملفات وحدود كل غرفة موثقة في `docs/WORKSTREAMS.md` وهي ملزمة.
- تكتب كل غرفة تقريرها في `docs/handoffs/WS-XX.md` وفق `docs/HANDOFF_TEMPLATE.md`، ولا تعدّل `docs/PROJECT_STATUS.md` أو `docs/ACHIEVEMENT_TRACKER.md` مباشرة.
- تحتاج تعديلات `package.json` وملف القفل وإعدادات TypeScript وأي ملف مشترك إلى موافقة `WS-00` قبل التنفيذ.
- إذا احتاجت غرفة ملفًا تملكه غرفة أخرى، تسجل الطلب في تسليمها ولا تعدّل الملف مباشرة.

## معيار الإنجاز

الحالات الرسمية الوحيدة هي: `لم يبدأ`، `جارٍ`، `متحقق`، `مدمج`، `منشور`.

لا تصبح المهمة `متحقق` إلا إذا توفر ناتج واضح، واختبار قبول ناجح، ودليل قابل للمراجعة، وقيمة للمستخدم، وذكر صريح لما لم يكتمل. إنشاء خطة أو branch أو worktree تأسيس تشغيلي وليس ميزة منتج.

قبل كل مهمة داخل غرفة:

1. حدد الناتج القابل للرؤية أو الاختبار واختبار القبول.
2. اذكر حتى ثلاثة أسباب محتملة للفشل، وحوّل كل سبب إلى فحص وقائي.
3. نفّذ مهمة واحدة قابلة للإغلاق واختبرها.
4. أنشئ commit مستقلًا واكتب بطاقة تسليم إلى `WS-00`.

## الملفات الأساسية

- `app/page.tsx`: حالات العرض، وضع العرض المضمون، وضع المايك المتصفحي التجريبي، وتشغيل الصوت.
- `app/globals.css`: التصميم المعتمد، تجاوب الهاتف، ومساحات الأمان والتمرير في حالة المراجعة.
- `app/api/tts/route.ts`: نقطة الخادم `POST /api/tts`، التحقق من `scriptId`، والـcache داخل الذاكرة.
- `lib/voice/scripts.ts`: النصوص الصوتية الثابتة المسموح بها؛ النص الحالي هو `coffee-v1`.
- `lib/voice/elevenlabs.ts`: اتصال ElevenLabs من الخادم والمهلة ومعالجة الأخطاء الآمنة.
- `lib/voice/playback.ts`: ترتيب بدائل التشغيل.
- `tests/`: اختبارات المسار الصوتي، العرض، والأمان.
- `.env.example`: أسماء متغيرات البيئة غير السرية.

## قواعد النطاق

- نفّذ التغيير المطلوب فقط؛ لا تضف خصائص أو تعيد تصميم الواجهة من تلقاء نفسك.
- لا تبدأ STT أو LLM أو محادثة ديناميكية أو تسجيل دخول أو قاعدة بيانات أو دفع أو تقارير دون طلب صريح.
- يوجد وضع مايك اختياري يعتمد على `SpeechRecognition` في المتصفح عند توفره، لكنه ليس مسار STT إنتاجيًا ولا خدمة خادمية.
- لا تغيّر النص الثابت `coffee-v1` أو ترتيب الحالات أو بدائل الصوت إلا بطلب واضح.
- لا تضف ملف `public/audio/coach-response.mp3` إلا إذا زوّد المستخدم ملفًا معتمدًا أو طلب إنشاءه صراحةً.
- المشروع يحتوي `.openai/hosting.json` ويستخدم Vinext/Vite وCloudflare Sites؛ حافظ على هذا المسار ولا تحوله إلى مشروع جديد.

## الأمان والأسرار

- لا تفتح أو تطبع أو ترفع `.env.local`.
- لا تضع `ELEVENLABS_API_KEY` أو أي مفتاح API في كود العميل أو المستودع أو مجلد `public`.
- الواجهة ترسل `scriptId` فقط إلى الخادم؛ لا ترسل نصًا حرًا أو أسرارًا إلى المتصفح.
- اقرأ إعدادات ElevenLabs من بيئة الخادم فقط.
- أبقِ رسائل أخطاء المزود عامة، ولا تسجل المفاتيح أو ترويسات الطلب الحساسة.
- `.env.example` قالب بلا مفتاح حقيقي، ويجب أن يبقى آمنًا للرفع.

## مسار الصوت المعتمد

عند حالة `speaking`:

1. ترسل الواجهة `{ "scriptId": "coffee-v1" }` إلى `POST /api/tts`.
2. يتحقق الخادم من المعرّف ويبحث في cache الذاكرة.
3. عند الحاجة، يولد ElevenLabs ملف MP3 باستخدام متغيرات بيئة الخادم.
4. عند التعذر، ينتقل التشغيل بالترتيب إلى:
   - `/audio/coach-response.mp3` (غير موجود حاليًا).
   - `speechSynthesis` في الجهاز.
   - مؤقت احتياطي يحافظ على اكتمال التسلسل.

لا تغيّر ترتيب البدائل دون سبب موثق واختبارات مناسبة.

## التصميم والتجاوب

- الاتجاه العام عربي RTL، والجمل الإنجليزية فقط LTR.
- لا تجمع العربية والإنجليزية داخل عنصر واحد إذا تسبب ذلك في خلل اتجاه.
- استخدم ارتفاع نافذة الهاتف الديناميكي `100dvh`، وامنع التمرير الأفقي.
- يجب ألا تغطي منطقة التحكم بطاقة التصحيح في `review`.
- حافظ على دعم safe area، بما في ذلك `env(safe-area-inset-bottom)`.
- افحص تغييرات الواجهة على الأقل عند: `390×844` و`360×740` و`430×932`.

## أوامر التحقق

المتطلب هو Node.js 22.13 أو أحدث. بعد أي تعديل برمجي مناسب شغّل:

```bash
npm exec -- tsc --noEmit
npm run lint
npm test
npm run build
```

ملاحظة: `npm test` يشغّل البناء أولًا. سكربتا `lint` و`build` يعتمدان على Bash ضمن بيئة Vinext/Cloudflare Sites.

للتغييرات التوثيقية فقط، راجع `git diff --check` ومحتوى الملفات، ولا يلزم إعادة بناء التطبيق ما لم يطلب المستخدم ذلك.

## قبل التسليم

- راجع `git diff` و`git status --short`.
- تأكد أن التغيير داخل النطاق المطلوب فقط.
- لا ترفع `.env.local` أو `node_modules` أو `.next` أو `dist` أو مخرجات البناء.
- اذكر الملفات المعدلة ونتائج التحقق وأي نقص متبقٍ بوضوح.

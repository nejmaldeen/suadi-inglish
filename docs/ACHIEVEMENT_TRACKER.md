# متتبع الإنجاز الحقيقي

آخر تحديث: 21 أغسطس 2026

## الهدف الحالي للمنتج

إظهار موقف واحد واضح ومقنع لمتعلم سعودي يطلب القهوة بالإنجليزية، عبر التسلسل `listening → thinking → speaking → review`، بصوت طبيعي وآمن من الخادم مع استمرار العرض عند تعذر مزود الصوت.

## تعريف الحالات

- `لم يبدأ`: لم يبدأ تنفيذ الناتج المطلوب.
- `جارٍ`: بدأ العمل ولم يستوف اختبار القبول والدليل بعد.
- `متحقق`: الناتج منفذ واختبار القبول ناجح وله دليل، لكنه ليس بالضرورة في `main`.
- `مدمج`: الناتج موجود في `main`.
- `منشور`: الناتج يعمل في بيئة يستخدمها المستخدم.

## لوحة الحالة

| البند | النوع | الحالة | الدليل الحالي | القيمة للمستخدم | ما لم يكتمل |
| --- | --- | --- | --- | --- | --- |
| رفع المشروع وربطه بـGitHub | تأسيس مستودع | `مدمج` | `main` و`origin/main` كانا عند `f7e985848e964527a025607bdc9e97670e893b95` وقت الفحص | حفظ العمل وإتاحة الدمج المنضبط | لا يثبت النشر للمستخدم |
| سلامة TypeScript وlint والاختبارات والبناء | تحقق تقني | `متحقق` | إعادة تحقق 21 أغسطس 2026: TypeScript وlint والبناء ناجحة والاختبارات 52/52 | تقليل مخاطر كسر العرض | لا يثبت جودة الصوت البشرية أو النشر |
| تشغيل ElevenLabs الحقيقي محليًا | ميزة منتج محلية | `متحقق` | تقرير التشغيل المحلي واستجابة `/api/tts` وسجل المصدر `elevenlabs` | سماع رد المدرّب بصوت مولد من الخادم | ليس دليل نشر ولا اعتمادًا لجودة الصوت |
| قابلية إعادة الاتصال الخارجي على بيئة جديدة | قابلية تشغيل | `لم يبدأ` | لا يوجد إعداد قابل لإعادة الإنتاج مثبت حتى الآن | تشغيل موثوق خارج البيئة الحالية | اختبار جهاز أو بيئة جديدة وتوثيق النتيجة |
| اعتماد طبيعية وهوية الصوت النهائية | جودة منتج | `جارٍ` | لم يقدم المستخدم اعتماد قبول نهائيًا | يجعل التجربة مقنعة ومناسبة للسياق السعودي | مقارنة واعتماد بشري موثق |
| تأسيس غرف العمليات | تأسيس تشغيلي | `جارٍ` | وثائق القيادة قيد الإنشاء من نقطة `f7e9858` | يمنع تداخل العمل وفقد الأدلة | الدمج وإنشاء وفحص الفروع والـworktrees |
| مكوّن التسجيل وSTT والخادم | أساس ميزة منتج | `مدمج` | merge `WS-02` إلى `main`: Push-to-Talk مستقل و`POST /api/stt` وموصل Scribe واختباراته | يوفّر مسارًا خادميًا آمنًا لتحويل الصوت إلى نص | لا يثبت وحده قبول الواجهة أو النشر |
| اتصال Scribe v2 الحقيقي | تكامل مزود محلي | `متحقق` | طلب حي واحد في 21 أغسطس 2026: `200 / success` وtranscript `Hello world` بلا كشف المفتاح | يثبت أن المزود والمفتاح يدعمان التفريغ الحقيقي | ليس اختبار قبول للمايك داخل الواجهة |
| ظهور transcript من الميكروفون داخل `YOU SAID` | قبول واجهة | `مدمج` | WS-04 commit `30b92c627860d589e7fbbe3b4fedac1c9bcb2f5e` وmerge بعنوان `merge: integrate WS-04 live speech input`؛ يعرض `completed.transcript` الخام | يعرض للمستخدم ما قاله فعليًا من دون تصحيح صامت | بطاقة واحدة تستبدل السابقة؛ لا يوجد سجل متعدد البطاقات |
| ميزة الإدخال الصوتي الكاملة لهذه المرحلة | ميزة منتج | `مدمج` | Push-to-Talk حقيقي ومحاولات مستقلة وحالات معالجة وخطأ وإعادة محاولة، مع قبول WS-04 البشري والآلي | يتيح دورة تعلم بصوت المستخدم الحقيقي | ليست منشورة، ودقة كلمات مفردة تبقى موضع مراقبة |
| أساس محرك المدرب الخادمي | أساس ميزة منتج | `مدمج` | دمج سلسلة `WS-03`: `5799ba9` ثم `26e3d12` ثم `d024704` عبر merge commit `merge: integrate WS-03 coach engine` | يوفّر تحليلًا وتصحيحًا وردًا مخصصًا بعقد صارم | لم يُوصل إلى `app/page.tsx` أو TTS |
| اتصال Gemini الحقيقي | تكامل مزود محلي | `متحقق` | طلبا قبول حقيقيان أعادا `200`: تصحيح `go` إلى `went`، والحفاظ على `vote` مع طلب الاستيضاح | يثبت أن المدرب يولد نتيجتين مختلفتين اعتمادًا على كلام المتعلم | ليس قبولًا داخل الواجهة ولا دليل نشر |
| ظهور رد المدرب الديناميكي داخل الواجهة | قبول واجهة | `لم يبدأ` | عقد `POST /api/coach` جاهز، لكن `app/page.tsx` لا يستدعيه | يعرض التصحيح والشرح المخصصين للمستخدم | يحتاج مهمة مستقلة داخل `WS-04` ثم تنسيق TTS مع الغرفة المالكة |
| سجل المحادثة متعدد البطاقات | توسع واجهة | `لم يبدأ` | تصميم WS-04 الحالي يستبدل بطاقة `YOU SAID` السابقة عمدًا | يتيح الرجوع إلى محاولات متعددة | ليس ضمن نطاق هذه المرحلة |

## الإنجازات المتحققة

- مسار العرض الأساسي ومسار TTS الخادمي وبدائل التشغيل موجودة في نقطة الأساس، مع أدلة الاختبار المسجلة في تقرير المشروع.
- تشغيل ElevenLabs الحقيقي تحقق محليًا في البيئة الحالية فقط.
- الفرع الرئيسي مرتبط بالمرجع البعيد، وكان المحلي والمرجع المحلي لـ`origin/main` متطابقين عند بداية التأسيس.
- اتصال Gemini الحقيقي تحقق بطلبين `200`، وأساس محرك المدرب أصبح موجودًا في `main`.
- الإدخال الصوتي الحقيقي موصول بالواجهة، ويظهر transcript الخام داخل `YOU SAID` مع محاولة مستقلة قابلة لإعادة المحاولة.

## العمل الجاري

- اعتماد طبيعية وهوية الصوت النهائية.
- تأسيس نظام غرف العمل والملكية والتسليم والقياس، ثم إنشاء worktrees المنفصلة.

## ما لم يبدأ

- إثبات قابلية إعادة الاتصال الخارجي في بيئة جديدة.
- توصيل محرك المدرب الديناميكي إلى الواجهة.
- TTS الديناميكي وسجل المحادثة متعدد البطاقات.
- أي توسع إلى سيناريوهات متعددة أو حسابات أو قاعدة بيانات أو دفع أو تقارير.

## العوائق والمخاطر

- جودة الصوت النهائية تحتاج حكم قبول بشري من المستخدم؛ النجاح التقني لا يكفي.
- السماح بالاتصال الخارجي قد يكون خاصًا ببيئة التشغيل الحالية وغير قابل لإعادة الإنتاج بعد.
- العمل المتوازي قد يتداخل في `app/page.tsx` أو ملفات الإعداد إذا لم تلتزم الغرف بالملكية.
- دقة كلمة `vote/order` ملاحظة مراقبة تخص WS-02 إذا تكررت؛ لا يطبق WS-04 استبدالًا صامتًا أو post-processing على transcript الخام.
- محرك المدرب مدمج خادميًا، لكنه غير موصول بالواجهة أو TTS بعد.
- cache الصوت داخل ذاكرة العملية فقط، ولا يثبت سلوكًا موزعًا أو دائمًا.

## الخطوة التنفيذية التالية الوحيدة

افتح مهمة مستقلة في `WS-04 — تجربة الجلسة والواجهة` لتوصيل استجابة `/api/coach` بالواجهة فقط؛ يبقى TTS الديناميكي خارج هذه الخطوة.

## السجل الزمني للأدلة

| التاريخ | البند | الحالة | الدليل | مراجعة ما لم يكتمل |
| --- | --- | --- | --- | --- |
| 19 أغسطس 2026 | نقطة الأساس للعرض ومسار الصوت | `مدمج` | commit `391279b3d5d58c69f99e6869bb1642f13d16aebb` | لا يثبت وحده اعتماد الصوت أو النشر |
| 19 أغسطس 2026 | تشخيص مصدر الصوت | `مدمج` | commit `f7e985848e964527a025607bdc9e97670e893b95`؛ التغيير في `app/page.tsx` فقط | لا ينسب إليه كامل إصلاح الصوت |
| 20 أغسطس 2026 | جرد النسخ السابقة | `متحقق` | بصمات SHA-256 في `docs/archive/pre-operations/MANIFEST.md` | الدمج وإنشاء worktrees لم يكتمل وقت السجل |
| 20 أغسطس 2026 | إعادة فحوص المشروع | `متحقق` | Node `v24.17.0`؛ `npm exec -- tsc --noEmit` ناجح؛ `lint=0`؛ الاختبارات 7/7؛ `build=0` مع تحقق Sites artifact | الفحص محلي وليس دليل نشر أو اعتماد صوت |
| 21 أغسطس 2026 | اتصال Scribe v2 الحقيقي | `متحقق` | طلب واحد من بيئة `WS-02`: `200 / success` وtranscript `Hello world`؛ لم يُكشف المفتاح | تحقق ظهوره داخل `YOU SAID` لاحقًا عبر دمج WS-04 |
| 21 أغسطس 2026 | أساس التسجيل وSTT الخادمي | `مدمج` | دمج commits `5d33114` و`44e5eb5` و`6822bbb` مع تحديث تسليم `WS-02` | اكتمل قبول الواجهة لاحقًا عبر دمج WS-04 |
| 21 أغسطس 2026 | اتصال Gemini الحقيقي | `متحقق` | طلبان حقيقيان أعادا `200` مع تصحيح `went` وحالة استيضاح `vote`، بلا كشف مفاتيح | لم يظهر الرد الديناميكي داخل الواجهة |
| 21 أغسطس 2026 | أساس محرك المدرب الخادمي | `مدمج` | سلسلة WS-03 `5799ba9` و`26e3d12` و`d024704` وmerge commit بعنوان `merge: integrate WS-03 coach engine` | التوصيل بالواجهة وTTS لم يبدأ |
| 21 أغسطس 2026 | الإدخال الصوتي الحقيقي داخل الواجهة | `مدمج` | WS-04 commit `30b92c627860d589e7fbbe3b4fedac1c9bcb2f5e` وmerge بعنوان `merge: integrate WS-04 live speech input`؛ الفحوص 52/52 | بطاقة `YOU SAID` واحدة تستبدل السابقة؛ الرد الديناميكي وTTS الديناميكي لم يبدآ |

## مراجعة ما بعد الدمج

بعد كل دمج إلى `main`، تضيف `WS-00` سطرًا إلى السجل الزمني يذكر commit، واختبار القبول، ونتائج التحقق، والقيمة للمستخدم، وما بقي. ثم تراجع لوحة الحالة ولا ترفعها آليًا من `متحقق` إلى `مدمج` أو `منشور` دون دليل مطابق.

## المراجعة الأسبوعية المختصرة

مرة أسبوعيًا، تسجل `WS-00`:

- ما انتقل بين الحالات مع الدليل.
- أهم عائق أو خطر تغير.
- العمل الجاري الفعلي، وليس الخطط المكتوبة.
- الخطوة التنفيذية التالية الوحيدة.

إذا لم يتغير شيء، يسجل ذلك صراحة من دون نسبة تقدم تخمينية.
